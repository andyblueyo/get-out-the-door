# Supabase — the publishable key is designed to ship in the browser bundle.
# RLS is the security boundary. Never put the secret/service_role key here.
NEXT_PUBLIC_SUPABASE_URL=https://htctfxlrskdztpirylez.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_ExzCs1Zg-vJatYi0l9wNBg_8-cX3ArR

# MTA GTFS-Realtime — free developer account required, no NEXT_PUBLIC_ prefix:
# this must stay server-only (lib/subway/realtime.ts reads it, never the
# browser). Get a key at https://api.mta.info/.
MTA_API_KEY=
